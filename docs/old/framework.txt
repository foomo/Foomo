Framework capabilities and features. Obviously there is much more \
functionality in the framework. At this point we recommend, that you stick \
with the parts, that are documented here.

What about namespaces?

We are working on them and for the forseeable future we are simply going to \
extend the non namespace class with a namespaced class.

That means, that RadHTMLDocument will be \Foomo\HTMLDocument ...

++ Avalanche caching
++ RadCliCall command line calls
++ RadConfig configuration and resources
++ RadDomainConfig domain specific configurations
++ Logger advanced logging
++ RadHTMLDocument html handling
++ RadMailer sending mail
++ RadMVC MVC
++ RadTimer tracking performance
++ RadTranslation translation
++ SWFObject wrapping swfs
